apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontierx.fullname" . }}
  labels:
    {{- include "frontierx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "frontierx.selectorLabels" . | nindent 4 }}
